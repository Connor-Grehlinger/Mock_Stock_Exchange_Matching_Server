#include <Poco/ThreadTarget.h>
// Classes for testing scalability (generating load)


/*
class ExchangeRequest : public Poco::ThreadTarget{
private:

public:

  ExchangeRequest(Poco::ThreadTarget::Callback method);

  ~ExchangeRequest();
  
  // Each thread to carry out this method
  virtual void run();

};  
*/
